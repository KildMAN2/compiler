float average(a, b, c: float) {
    return (a + b + c) / (float)3;
}

void main() {
    x, y, z: float;
    result: float;
    read(x);
    read(y);
    read(z);
    result = average(x, y, z);
    write(result);
}
