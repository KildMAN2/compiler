void main() {
    x, y, result: int;
    read(x);
    read(y);
    if (x > 5) then {
        result = 1;
    } else {
        result = 0;
    }
    if (y > 3) then {
        result = result + 10;
    }
    write(result);
}
