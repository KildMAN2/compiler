// Test early return in function
int check(int x) {
    result:int;
    
    if (x < 0) then {
        return 0;
    }
    
    result = x * 2;
    return result;
}

void main() {
    write(check(x:5));
    write(check(x:-3));
}
