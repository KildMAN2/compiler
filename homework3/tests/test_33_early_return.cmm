// Test early return in function
int check(int x) {
    if (x < 0) then {
        return 0;
    }
    
    int result;
    result = x * 2;
    return result;
}

void main() {
    write(check(x:5));
    write(check(x:-3));
}
