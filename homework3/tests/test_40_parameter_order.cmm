// Test named parameters in different orders
int compute(int a, int b, int c) {
    return a * 100 + b * 10 + c;
}

void main() {
    write(compute(a:1, b:2, c:3));
    write(compute(c:3, a:1, b:2));
    write(compute(b:2, c:3, a:1));
}
