// Test complex boolean expressions
void main() {
    a:int;
    b:int;
    c:int;
    
    a = 5;
    b = 10;
    c = 15;
    
    if (a < b) and (b < c) then {
        write("Ascending order\n");
    }
    
    if (a > 0 and b > 0) or c < 0 then {
        write("Complex condition true\n");
    }
    
    if not (a > 20 or b > 20) then {
        write("Both less than 20\n");
    }
}
