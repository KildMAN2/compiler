// Test logical operators with multiple conditions
void main() {
    int a;
    int b;
    int c;
    
    a = 5;
    b = 10;
    c = 15;
    
    if ((a < b) and (b < c)) then {
        write(1);
    }
    
    if ((a > b) or (b < c)) then {
        write(2);
    }
    
    if (not (a > b)) then {
        write(3);
    }
}
