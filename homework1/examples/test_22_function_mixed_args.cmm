# Test: Function calls with mixed positional and named arguments
int compute(a:int, b:int, c:int) {
    return a + b * c;
}

void main() {
    result:int;
    result = compute(1, c:3, b:2);
    result = compute(5, 10, c:15);
}
