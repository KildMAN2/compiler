# Test: Boolean OR operations
void main() {
    x:int;
    y:int;
    
    if x == 0 || y == 0 then
        write("At least one is zero");
        
    if x > 100 || y > 100 || x < 0 then
        write("Out of range");
}
