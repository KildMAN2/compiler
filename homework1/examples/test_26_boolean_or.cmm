# Test: Boolean OR operations
void main() {
    x:int;
    y:int;
    
    if x == 0 or y == 0 then
        write("At least one is zero");
        
    if x > 100 or y > 100 or x < 0 then
        write("Out of range");
}
