# Test: Boolean AND operations
void main() {
    x:int;
    y:int;
    
    if x > 0 && y > 0 then
        write("Both positive");
        
    if x < 10 && y < 10 && x > 0 then
        write("All conditions met");
}
