# Test: Boolean AND operations
void main() {
    x:int;
    y:int;
    
    if x > 0 and y > 0 then
        write("Both positive");
        
    if x < 10 and y < 10 and x > 0 then
        write("All conditions met");
}
