# Test: Type mismatch in comparison should be semantic error
# Expected: Semantic error (type mismatch)
# Bug: May report syntax error instead of semantic error

void main() {
    x:int;
    y:float;
    
    x = 10;
    y = 5.5;
    
    # Type mismatch in comparison - should be semantic error
    if x < y then
        write("True\n");
}
