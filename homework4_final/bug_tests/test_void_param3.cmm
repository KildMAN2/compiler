/* Test: Grouped parameters with void type
 * Expected: Semantic error (parameters cannot be void)
 * Bug: No check for void parameter types
 */

float compute(x, y, z:void) {
    return 1.0;
}

void main() {
    write(compute(1, 2, 3));
}
